Drop your real resume here as `resume.pdf`.

The "Download Resume" button on the Contact section serves /resume.pdf
directly from this folder. Vite's public/ directory is copied verbatim into
the build, so anything you put here is reachable at the site root.

Steps:
  1. Save your latest resume as `resume.pdf` in this folder.
  2. Delete this README (optional but tidier).
  3. Push and redeploy.
